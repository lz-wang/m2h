# AGENTS.md

本文件是仓库协作入口，只保留所有任务都需要遵守的规则。按需阅读其余文档：

- 面向用户的命令契约与示例：`README.md`
- 当前状态、阶段任务与验收标准：`PROGRESS.md`
- 已发布的新特性与故障修复：`CHANGELOG.md`

## 工作原则

- 开始前先阅读 `PROGRESS.md`，确认当前阶段、依赖和非目标。
- 先理解现有调用链和测试，再做范围最小、可验证的修改。
- 一个提交只包含一个逻辑变更；提交信息使用中文 Conventional Commits。
- 不提交密钥、令牌、个人环境配置、构建产物或覆盖率文件。
- 未实现的能力必须标注为规划，不得在文档中宣称已经可用。
- 新设计决策或范围变化应同步更新 `PROGRESS.md`。

## 架构边界

- `internal/markdown` 是 Markdown 解析、扩展、链接重写和 HTML 渲染的唯一核心。
- `convert` 与 `preview` 必须复用该核心，不得维护两套渲染规则。
- CLI 参数校验在执行文件或网络操作前完成；未知参数和非法组合必须返回错误。
- Go 后端负责文件系统、Markdown 渲染和 HTTP API；React 只负责目录预览 UI。
- Web 构建产物由 Go 嵌入；前后端 API 契约变更必须同时更新测试与文档。
- 文件路径统一以输入根目录为边界，面向 API 和 glob 的相对路径使用 `/` 分隔。

## Go 约定

- 使用 `gofmt` 与 `goimports-reviser`，遵循标准库优先的导入顺序。
- 包保持职责单一；除入口装配外，业务逻辑放在 `internal/`。
- 错误应包含操作与目标上下文，并保留底层错误以支持 `errors.Is/As`。
- 文件遍历、符号链接、监听和 HTTP handler 必须覆盖成功与失败路径测试。
- 默认安全：不启用 raw HTML，不越过输入根目录，不跟随目录内符号链接递归。

## Web 约定

- 使用严格 TypeScript，不以 `any` 或非空断言绕过边界校验。
- 使用 Biome 统一检查与格式化，Vitest 覆盖状态和交互行为。
- 优先复用 shadcn/base-ui 组件和 lucide 图标，保留键盘操作与可访问名称。
- Markdown 正文使用 `github-markdown-css`，布局宽度与 padding 遵循 README 契约。
- React 不重新解析服务端已返回的 HTML 来推导标题；标题由 API 明确提供。

## 验证与交付

- 文档或配置变更至少检查 diff、格式和目标语法。
- Go 变更运行相关单测；Web 变更运行相关 lint、测试和构建。
- 后端（Go）测试覆盖率不得低于 90%，前端测试覆盖率不得低于 70%。
- 跨层或发布相关变更运行 `make check`；发布前运行完整 CI 和六平台构建。
- 若某项验证无法运行，交付说明必须写明未验证项、原因和建议命令。
- 发布条目仅记录新特性和故障修复，并与 GitHub Release 摘要保持一致。
