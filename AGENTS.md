# AGENTS.md

本文件是仓库协作入口，只保留所有任务都需要遵守的规则。按需阅读其余文档：

- 面向用户的命令契约与示例：`README.md`
- 新特性与故障修复的未发布及发布记录：`CHANGELOG.md`

## 工作原则

- 开始前先阅读 `README.md` 与 `CHANGELOG.md`，确认当前命令契约与未发布变更。
- 先理解现有调用链和测试，再做范围最小、可验证的修改。
- 一个提交只包含一个逻辑变更；提交信息使用中文 Conventional Commits。
- 提交只能署名为仓库所有者；禁止添加任何其他协作者或署名（包括但不限于 Claude、Codex 等），不得在提交信息中加入 `Co-authored-by`、`Signed-off-by` 等协作者 trailer。
- 不提交密钥、令牌、个人环境配置、构建产物或覆盖率文件。
- 未实现的能力必须标注为规划，不得在文档中宣称已经可用。

## 架构边界

- `internal/markdown` 是标准 GFM 解析、链接重写和 HTML 渲染的唯一核心。
- HTML export 与 Web 文档服务必须复用 `internal/markdown`，不得维护两套 Markdown 渲染规则。
- CLI 参数校验在执行文件或网络操作前完成；未知参数和非法组合必须返回错误。
- Go 后端负责文件系统、Markdown 渲染和 HTTP API；React 只负责目录预览 UI。
- Web 构建产物由 Go 嵌入；前后端 API 契约变更必须同时更新两端实现与测试。
- 文件路径统一以输入根目录为边界，面向 API 和 glob 的相对路径使用 `/` 分隔。

## Go 约定

- 使用 `gofmt` 与 `goimports-reviser`，遵循标准库优先的导入顺序。
- 包保持职责单一；除入口装配外，业务逻辑放在 `internal/`。
- 错误应包含操作与目标上下文，并保留底层错误以支持 `errors.Is/As`。
- 文件遍历、符号链接、监听和 HTTP handler 必须覆盖成功与失败路径测试。
- 输入边界安全：Markdown 中的 raw HTML 按输入原样渲染；不越过输入根目录，不跟随目录内符号链接递归。

## Web 约定

- 使用严格 TypeScript，不以 `any` 或非空断言绕过边界校验。
- 使用 Biome 统一检查与格式化，Vitest 覆盖状态和交互行为。
- 优先复用 shadcn/base-ui 组件和 lucide 图标，保留键盘操作与可访问名称。
- Markdown 正文使用 `github-markdown-css`；用户可见的主题与正文宽度选项必须与 README 保持一致。
- React 不重新解析服务端已返回的 HTML 来推导标题；标题由 API 明确提供。

## 文档与发布同步

- 功能范围、设计决策、依赖、技术基线或已知风险变化时，必须同步更新受影响的代码、测试、workflow 与说明，不得保留失效描述。
- `README.md` 只保留面向用户的核心命令、关键契约与简明特性概览；检查规则、复杂语法和长示例等细节优先维护在 `docs/demos/`，README 使用相对链接引用，不得重复维护大段清单导致篇幅膨胀。
- CLI、API、默认值、错误消息、输出格式或使用示例变化时，必须同步相关测试与 `README.md`；能力从规划变为可用时同步移除未实现提示。
- 支持的 Markdown/GFM 特性、渲染方式或用户可见兼容性限制变动时，必须同步更新 `README.md` 的“Markdown 支持”概览、相关 `docs/demos/` 说明及入口链接。
- 前后端 API 契约、依赖与锁文件、嵌入资产、支持平台、构建或发布流程变化时，必须同步所有受影响的代码、测试、workflow 和文档。
- 面向用户的新特性和故障修复必须在同一变更中写入 `CHANGELOG.md` 的 `[未发布]`；纯重构、测试、构建和文档维护不写发布条目，除非改变用户可见行为。
- 发布前将 `[未发布]` 的实际条目移入对应版本，确保版本号、日期、tag、二进制版本和归档名称一致。
- GitHub Actions 创建或更新 GitHub Release 时，标题必须与 tag 对应，正文必须逐字使用 `CHANGELOG.md` 对应版本的发布条目；发布后必须核对，不一致视为发布未完成。

## 验证与交付

- 文档或配置变更至少检查 diff、格式和目标语法。
- Go 变更运行相关单测；Web 变更运行相关 lint、测试和构建。
- 后端（Go）测试覆盖率不得低于 90%，前端测试覆盖率不得低于 70%。
- 跨层或发布相关变更运行 `make check`；发布前运行完整 CI 和六平台构建。
- 推送代码到远程仓库前，必须确保 `make test` 通过且 `make build` 成功；任一命令失败时不得推送。
- 若某项验证无法运行，交付说明必须写明未验证项、原因和建议命令。
