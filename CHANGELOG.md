# 更新日志

本文件记录每次版本发布包含的新特性和故障修复，发布内容应与对应的 GitHub Release 摘要一致。

格式遵循 [Keep a Changelog 1.1.0](https://keepachangelog.com/zh-CN/1.1.0/)，正式版本遵循[语义化版本 2.0.0](https://semver.org/lang/zh-CN/)。正式版本格式为 `x.y.z`；开发版本格式为 `dev-<commit date>-<commit7>`。

每个版本只使用以下分类：

- `新增`：面向用户的新特性。
- `修复`：面向用户的故障修复。
- `移除`：面向用户的特性或能力移除。

内部重构、测试、构建和文档维护不进入发布摘要，除非它们直接交付新特性或修复用户可见故障。

## [未发布]

### 新增

- WebUI 中加载失败的图片现在原地折叠为应用自带的占位图（`/image-load-failed.svg`），不再留出破损的图片图标：占位保持原文档位置的固定阅读尺寸，顶部资源告警继续报告原始引用路径（而非替换后的占位地址），占位不提供 Lightbox 放大（放大按钮隐藏、点击不打开），也不再显示名称与尺寸提示；占位资源自身的加载失败被标记拦截，不会形成错误循环。
- WebUI 图片悬停提示在名称之外新增第二行元数据，显示图片的原始尺寸与格式（如 `1920 × 1080 · PNG`）：尺寸取自图片自身像素（`naturalWidth`/`naturalHeight`），与页面渲染尺寸无关；格式从图片 URL 扩展名或 data URL MIME 推导（`jpeg/jpg/jpe` 统一显示 `JPG`，另支持 PNG、SVG、WEBP、GIF、AVIF，未知扩展名原样大写显示），不发出任何额外网络请求；没有 alt 文本的图片也显示尺寸与格式行。提示仍为悬停/键盘聚焦时浮现、`aria-hidden`、`20rem` 宽度上限，不改变原有定位与溢出行为。
- WebUI 访问裸根地址 `/` 时自动打开工作区的默认文档：单文件预览仍打开其唯一文档；目录与多 root 工作区现在打开第一个 root 的 README（大小写不敏感），无 README 时依次回退到 index、按文件树相同自然名称排序的根级第一个 Markdown 文档；子目录中的 README/index 不参与选择，多 root 也只考虑第一个 root（第一个 root 无根级文档时不回退到后续 root）。地址随之通过 `replaceState` 规范化为对应 `/doc/...` 深链接，刷新与分享直达同一文档且不污染历史记录；显式 `/doc/...` 地址始终优先，不运行默认选择。

## [0.16.0] - 2026-09-03

### 新增

- 新增 Vega-Lite 统计图表支持：` ```vega-lite `（别名 ` ```vegalite `）围栏代码块中的 JSON spec 在 Web 文档服务与导出 HTML 中渲染为交互式 SVG 图表（Vega 6.4.0 / Vega-Lite 6.4.3 / Vega-Embed 7.1.0，WebUI 从内嵌运行时按需加载，导出从同一版本的 jsDelivr CDN 加载，普通文档不下载任何图表运行时）。spec 必须自包含：数据写在 `data.values`，引用外部数据的 spec（顶层 `data.url`、`datasets` 条目、lookup `from.data` 与分层/拼接/分面子图表中的同类位置）在嵌入前即被判定不支持并按失败隔离处理（保留源码文本、不渲染 SVG、不提供放大按钮），而不是渲染成没有数据 mark 的空图表；字符串 `config`/`patch` 与外部图片 URL 由宿主加载器在加载时拒绝且不发出任何网络请求，该契约由宿主策略而非页面 CSP 保证，导出 HTML 同样成立；文档也不能通过 `usermeta.embedOptions` 重开 Vega-Embed 的导出/编辑器菜单或改写渲染器（`mode` 固定 `vega-lite`、`renderer` 固定 SVG）。图表超链接（`href` 通道）的点击遵循与正文链接一致的策略：跨 origin HTTP(S) 在新标签页打开并带 `noopener noreferrer`，同源保持默认，`javascript:` 等其他协议一律拒绝。图表表达式通过 Vega-Embed 内嵌的 AST 解释器求值（`ast: true`）而非 Vega 默认的生成代码路径，WebUI 的 `Content-Security-Policy` 因此维持 `script-src 'self'` 不含 `'unsafe-eval'`，Markdown raw HTML 中的内联脚本与事件处理器依旧不可执行，导出 HTML 采用相同解释器策略。无效 JSON、非对象 spec 与编译失败的图表逐图隔离——保留源码文本、不提供放大按钮、仅控制台警告，不影响同文档其他图表与 Mermaid、公式。图表外观 chrome（背景透明、坐标轴/图例/标题文字与网格）跟随阅读器主题并从主题 CSS 变量取色，作者定义的数据配色与 scale 不被覆盖，切换主题时外框与放大按钮 DOM 身份保持；图表与图片、Mermaid 按文档顺序共用同一 Lightbox。语法示例与兼容矩阵见 [Vega-Lite 演示索引](docs/demos/vega-lite/00-index.md)。

### 修复

- 修复 WebUI 在图表运行时仍在加载时切换主题、或快速连续切换主题（浅色→深色→浅色）时 Vega-Lite 图表可能停留在源码块或渲染损坏的问题：初始富内容渲染与主题重绘此前共用同一渲染代序，主题切换会使尚未完成的初始渲染失效——此时图表容器尚未创建（Vega 运行时未加载完），主题重绘找不到重绘目标，` ```vega-lite ` 原始代码块因此不再转换；连续快速切换还可能让同一图表容器的两次嵌入并发执行、交错写入 DOM。现在初始渲染仅在切换文档时失效，主题重绘排队等待当前文档初始渲染完成后才执行，同一正文的多次主题重绘严格串行，排队期间被更新主题取代的中间重绘直接跳过；Mermaid 图表与 Vega-Lite 图表仍同步重绘，正文的 DOM 身份与焦点保持不变。

## [0.15.2] - 2026-08-30

### 新增

- Web 文档服务新增统一的浏览器安全响应头：所有响应（页面、API、附件、runtime 资源与 404/405）携带 `Content-Security-Policy`（`script-src 'self'`、`base-uri 'none'`、`object-src 'none'`、`frame-ancestors 'self'`、`form-action 'none'`、`frame-src 'none'` 等）、`X-Content-Type-Options: nosniff`、`Referrer-Policy: same-origin`、`X-Frame-Options: SAMEORIGIN` 与 `Permissions-Policy`（禁用摄像头/麦克风/定位/支付/USB）。`style-src` 暂保留 `'unsafe-inline'`（React、Mermaid 与 KaTeX 均产生内联样式），脚本全部来自本源 `/runtime/`，无需 CDN 或内联脚本；`Referrer-Policy: same-origin` 使点击外链时不会把私人文档地址作为 Referer 发给外部站点。m2h 不设置 HSTS（TLS 由反向代理终结，HSTS 归代理层）。页面因此移除了 WebUI 中唯一的内联脚本（scroll restoration 预设移入应用入口模块），并新增真实 Chromium 回归：Mermaid、ZenUML、KaTeX、可排序表格、SVG 与外部图片在严格 CSP 下全部正常渲染且无任何策略拒绝，恶意 raw HTML 的内联事件脚本（`onerror`）与内联 `<script>` 不再执行。

- HTTP 服务硬化以支持长期常驻运行：`ReadHeaderTimeout`（5s）与 `IdleTimeout`（60s）防止慢速头部与闲置连接无限占用，`MaxHeaderBytes`（1 MiB）限制请求头大小；`WriteTimeout` 刻意不设置，`/assets` 的大文件（PDF/视频/压缩包）下载不会被全局写超时截断，公网慢客户端由反向代理隔离。

- 新增 `GET/HEAD /healthz` 健康端点：返回 `200` 与 `ok`（`text/plain; charset=utf-8`、`Cache-Control: no-store`），其他方法返回 `405`。它只回答 HTTP 进程是否已初始化并可接受请求，不读取、不扫描任何文档——发布目录在 `git pull` 等瞬时变更期间不会导致健康检查失败与误重启。适合 systemd 与 Uptime Kuma 等本机监控直接探测 upstream。

### 修复

- 修复可见符号链接可绕过发布边界读取隐藏文件与主动 Web 内容的问题：发布策略此前只作用于请求路径，`public.md -> .secret.md` 这类指向 root 内隐藏目标的别名、以及 `safe.txt -> app.js` 这类伪装扩展名指向主动 Web 内容的别名都能在解析后绕过 hidden/active 策略。现在发布策略同时作用于请求路径与 canonical 解析目标：文档路由对解析结果复检隐藏属性（`--glob`/`--depth` 仍按读者看到的别名路径判定，浅层别名指向深层文档的既有语义不变），附件路由对解析结果重跑完整准入策略，目录扫描同样跳过 canonical 目标隐藏的别名——隐藏目标的别名不再出现在文件树中（此前会泄露其标题）。指向可见普通文件的合法别名（如 `logo.png -> images/logo-real.png`）行为不变；`m2h check` 的静态分析语义不变。

- 修复目录文档服务默认暴露点开头隐藏路径的问题：目录 root 下任何包含 `.` 开头路径段的内容（如 `.env`、`.git/config`、`.github/workflows/`、`foo/.private/file.pdf`）不再出现在文件树与文档 API 中，`/doc`、`/raw` 也不再响应这些路径；隐藏目录在扫描阶段即被跳过，不再进入遍历结果。显式以单文件路径启动（如 `m2h notes/.private.md`）仍正常服务该文件，`m2h check` 的静态分析语义不变，仍可检查隐藏 Markdown。

- 修复 `/assets` 附件路由可提供任意非 Markdown 文件的问题：HTML（`.html`/`.htm`/`.xhtml`）、脚本（`.js`/`.mjs`/`.cjs`）与样式表（`.css`）等主动 Web 内容现在返回 404，避免发布目录内的文件在 m2h origin 下成为同源可执行页面；隐藏路径同样不再可访问。图片、SVG、PDF、压缩包、音视频等普通被动附件不受影响，`Range` 分段请求（`206 Partial Content`）与 symlink 边界行为保持不变。`/assets` 响应统一携带 `Content-Security-Policy: sandbox; default-src 'none'`，直接导航到 SVG 附件时其中的内嵌脚本不会作为文档脚本执行；Markdown 中以 `<img>` 引用的 SVG 展示不受影响。

## [0.15.1] - 2026-08-30

### 新增

- Markdown 本地引用支持以单个 `/` 开头的当前文档 root 相对路径：`/docs/guide.md` 与 `/images/logo.png` 分别在 WebUI 中统一路由到当前 root 的 `/doc` 与 `/assets` 地址，Markdown 链接/图片及 raw HTML 的 `href`、`src`、`poster`、`data` 使用同一语义；`m2h check` 同步检查目标缺失、非普通文件、越界、symlink、文档服务范围与锚点。该路径绝不映射到宿主机文件系统根目录，多 root 模式始终锚定引用所在 root；`//host/path`、HTTP/HTTPS、`mailto:` 与 `tel:` 保持外部 URL 行为。

### 修复

- 修复 multi-root WebUI 中越界的文档相对链接仍可能被浏览器解析到其他 root 的问题：已经分类为本地、但无法在当前逻辑 root 内解析的 Markdown 链接/图片及 raw HTML URL 不再原样输出，而是改写到携带原始目标的专用 `/__m2h_invalid_local_reference__` 404 地址；普通点击、修饰键点击、中键和“在新标签页打开”因此都无法绕过 renderer 的 root 边界。外部、协议相对和纯片段 URL 保持原样。

## [0.15.0] - 2026-08-30

### 新增

- WebUI 文档中的跨站 HTTP/HTTPS 链接改为在新标签页打开：Markdown 链接、GFM autolink 与 raw HTML `<a>` 统一在浏览器端按页面 origin 判定，跨 origin（协议或端口不同同样视为跨站）的外链自动附加 `target="_blank"` 与 `rel="noopener"`，作者已有的 `rel` 标记（如 `nofollow`）保留不覆盖；本站链接、锚点与 `mailto:`/`tel:` 保持原有行为。本站判定使用浏览器实际访问的地址而非服务端视角，因此 Nginx 反向代理（HTTPS termination → HTTP upstream）部署下无需任何额外配置。导出 HTML 不受影响。

- `m2h check` 的文本报告在交互式终端中按等级着色：每条诊断只把 error 显示为红色、warning 显示为黄色；最终总结将“全部通过”显示为绿色，并分别为 error/warning 计数使用红色/黄色。路径、行列、规则名、消息、退出码及 JSON schema 不变，重定向、管道、`NO_COLOR` 与 `TERM=dumb` 保持无 ANSI 颜色。

- `m2h check` 新增 `--enable`/`--disable` 规则选择与 15 条新规则，规则总数扩展到 25 条。`--enable` 在默认规则之上追加、`--disable` 从中移除（逗号分隔，`--disable` 优先，`all` 指代全部规则，如 `--enable all --disable section.empty` 运行除该规则外的全部规则）；未知规则名在读取任何文件之前以 `Error: unknown check rule "foo.bar"` 失败。新增七条默认开启的 error：`reference.undefined`（显式 `[text][label]`/`[text][]` 引用无定义，裸 `[label]` 不报）、`footnote.undefined`（`[^label]` 无对应定义）、`footnote.empty`（脚注定义无内容，多行缩进续行不算空）、`table.column-mismatch`（表格行列数与分隔行不符——渲染时会被补空或截断，含表头被补空的情况；表头多于分隔线导致整表被拒绝时直接报告）、`html.comment-unclosed`（`<!--` 未闭合，其后内容整体渲染为注释）、`link.reversed`（高置信度 `(text)[url]` 反转链接写法，`f(x)[0]`、`array[index]` 不报）；新增四条默认开启的 warning：`heading.level-skip`（标题向下跳超过一级）、`heading.duplicate`（同一父 section 下重复标题）、`code-fence.language-missing`（fenced code 无语言）、`reference.unused`、`footnote.unused`（定义从未被引用）；新增四条默认关闭、经 `--enable` 开启的 warning：`section.empty`、`link.text-nondescriptive`、`unicode.mojibake`、`unicode.invisible-character`（后三条按多字符 mojibake 签名、中英文小词表精确匹配与"行首尾/邻接空白/连续出现/bidi 控制"保守触发，emoji 的 ZWJ 与 variation selector 不会误报）。引用与脚注的未定义/未使用判定由实际解析器给出（reference 标签与渲染器同一归一化比较、脚注标签逐字节比较），行内与 fenced 代码中的内容永不参与判定；所有正文诊断的行列号统一含 Frontmatter 偏移。文本与 JSON 输出契约不变，新规则沿用同一 schema。

- 新增 `m2h check` 子命令，检查 Markdown 文档的 Frontmatter、结构、本地引用、文档范围与锚点一致性：`m2h check <file|directory>` 支持 `--depth`/`--glob`（与浏览命令同一文档范围）、`--format text|json` 与 `--strict`。检查六类 error（Frontmatter 无效、本地目标缺失/非普通文件/越过根目录、Markdown 目标不在当前服务范围、锚点不存在）与四类 warning（图片缺 alt、多个 H1、日期字段非有效 ISO 日期、空 destination），行内与 fenced 代码中的 URL 不会误报；本地引用覆盖 Markdown 链接/图片、reference-style 链接与 raw HTML 的 href/src/poster/data。锚点与标题 ID、URL 解码、`/doc` 与 `/assets` 路由判定、symlink 安全边界均与 Web 浏览共用同一实现：图片与 raw HTML 的 src/poster/data 一律按 `/assets` 路由校验，该路由不提供 Markdown 文件，因此指向 `.md` 的此类引用（含 `guide%2Emd` 这类编码写法）会被报告为不可达；`sub%5Cdeep.md`、`images%2F.%2Flogo.png` 这类依赖服务端归一化的路径与浏览行为判定一致；Frontmatter 无法解析的目标文档不会再级联产生 anchor.missing 误报。诊断按 path:line:column 输出（raw HTML 引用定位到具体属性值所在行列，日期字段 warning 定位到该字段在 Frontmatter 中的真实行列）并附带统计摘要，JSON 结构稳定供 CI 消费；单文件模式下诊断路径显示用户输入的路径，symlink 输入不再显示解析后的目标文件名；发现 error（或 --strict 下存在 warning）时退出码为 1。

- Lightbox 新增鼠标滚轮平滑缩放：指针位于目标图片或 Mermaid 图表上时，向上滚动放大、向下滚动缩小；缩放量按实际滚动幅度连续计算，小幅触控板输入可细微调整，普通鼠标单次滚轮事件最多约改变 8.3%，避免快速跳级，同时保持 1–5 倍边界且不带动背景文档。工具栏按钮仍使用 1.25 倍步进，现有拖动、旋转和空白区域关闭行为不变。

- Mermaid ZenUML 时序图支持：WebUI 与导出 HTML 均可渲染 `zenuml` 图表。此前两端只加载 Mermaid Core，合法的 ZenUML 会退化为 `Syntax error in text`；现在 WebUI 在文档含 zenuml 图表时按需加载二进制内嵌的 mermaid-zenuml 0.2.3 运行时（保留上游 dist 相对路径的 chunks 目录），按 load → register → initialize → render 顺序注册 external diagram 后渲染，导出页面则在产物确含 zenuml 图表时携带同一 release 的 jsDelivr 插件 URL、由内联引导脚本动态导入注册。插件注册按页面单例缓存且失败不缓存 rejected Promise（主题切换重绘可重试）；普通 Mermaid 图表不产生插件下载；WebUI 图表渲染失败时在控制台输出图表类型与原始错误，使插件缺失、资源加载失败与语法错误可区分。

### 修复

- `m2h check` 在 error（或 `--strict` 下存在 warning）时仍以退出码 1 失败，但文本报告现在以统计摘要作为最后一行，JSON 报告保持以结构化对象结束，二者都不再由进程入口追加重复的 `Error: check found ...`；参数、文件系统与报告写入等真实执行错误仍保留原有错误消息。warning-only 在未启用 `--strict` 时仍保持退出码 0。

- 修复 `reference.undefined` 的上报数量与位置可能偏离解析器实际拒绝的问题：解析器记录的 reference lookup 失败从集合升级为按 label 计数，源扫描产生的 undefined 上报以该计数为上限；同时由真实 AST 标出已接受 inline link 的 destination/title 源范围并排除其中的假括号对，因此即使链接标题里的同名 `[text][label]` 早于真实失败出现，也不会占用诊断名额或把错误位置导向标题。

- 修复 `code-fence.language-missing` 与解析器的分歧：fence 的识别与语言判定改由真实 `FencedCodeBlock` AST 给出，此前逐行扫描只认顶层行首的 ```/~~~——blockquote、列表项内的无语言 fence 一直漏报，raw HTML 块内的 ``` 反被误当作 Markdown fence 上报；现在容器内的 fence 正确报告且行列定位到 opener 行，`<div>` 等 HTML 块内的反引号不再参与判定；无内容与 info string 的空 fence 在 `*`/`+` 列表项中同样定位到 opener，而不是误落到 closing fence。

- 修复三条默认开启的 error 规则对 raw HTML 的语义边界：`reference.undefined`、`footnote.undefined` 与 `link.reversed` 的源扫描跳过 HTML 注释块、行内注释、HTML 块及 inline raw HTML tag token，避免 `<!-- [^demo] -->` 与 HTML block 内示例被误报；但不再把 `<code>`/`<kbd>` 等一对 inline tag 之间的普通 Markdown 源文本整体豁免——Goldmark 会继续解析这部分内容，检查器现在与渲染语义一致，不再漏报其中的真实问题。`link.reversed` 的 destination 判定同步收紧为 scheme、路径或已知文件扩展名，`(version)[v1.2]` 这类版本号写法不再误报；`unicode.mojibake` 与 `unicode.invisible-character` 检查的是源文件质量，仍扫描 raw HTML，仅代码区域豁免。

- 修复 opt-in `section.empty` 把 thematic break 误判为“无渲染内容”的问题：`---`/`***` 会渲染为 `<hr>`，现在与段落、列表、表格和代码块一样计为章节内容；只有 reference definition、HTML comment 与空白仍不贡献渲染内容。

- 修复不带参数执行 `m2h export` 时输出 "No help topic for 'export'" 并以退出码 3 失败的问题：urfave/cli v3 的 ShowCommandHelp 需要在父命令中按名查找子命令，此前传入命令自身导致帮助从未显示；现在 `m2h export` 与 `m2h check` 不带参数时正常显示各自帮助并以退出码 0 结束。

- 修复同一段落中的多个美元金额被误渲染为行内公式的问题：KaTeX 的单美元自动分隔此前会把 `$9` 与后续 `$200` 直接配成一个跨句公式；现在 WebUI 与导出 HTML 都按 Markdown 常用边界识别 `$...$`（开启符右侧非空白，闭合符左侧非空白且右侧非数字），未配对的金额美元符号保持普通文本，合法行内与行间公式不受影响。

- 修复 ZenUML 页面破坏 WebUI 与导出 HTML 主题的问题：mermaid-zenuml 0.2.3 内含的 @zenuml/core 3.47.2 会在 external diagram 注册时向宿主 `<head>` 注入约 888 KiB 的无作用域样式，其中 `:root --background` 会覆盖 m2h 的同名主题变量，造成浅色模式正文为浅色但顶栏、目录仍为深色，且离开文档后污染继续残留。现在两端都将第三方 renderer 的 import/register 约束为无宿主 stylesheet 副作用边界，在成功与失败路径清理该阶段新增的全局样式；导出页还会在异步注册前关闭 Mermaid 内置的 window-load 自动扫描，避免多图文档抢先退化为语法错误。由于上游静态 SVG 固定使用浅色配色，m2h 会按当前模式为 SVG 注入仅限图内的主题标记和暗色色板，使参与者、消息、片段与注释在暗色页面中采用与 Mermaid 原生时序图一致的深色表面、浅色文字及线条，同时浅色 SVG 保持上游原貌。多图文档、浅色/深色模式、主题切换、页内导航与导出页面均不会再污染宿主页面。


## [0.14.0] - 2026-08-28

### 新增

- Lightbox 查看器新增进出场动画并完成语义泛化：打开时遮罩 160ms 淡入、内容区 180ms 缩放淡入（96% → 100%），关闭时反向退出后卸载——关闭按钮、Esc 与点击遮罩三类入口统一经 Base UI 对话框过渡生命周期驱动，动画播放完毕才清理状态；缩放、旋转与拖动的实时 transform 保持无过渡，拖动跟手、缩放钳位与控件层级行为不变；组件由 ImageLightbox 泛化为 DocumentLightbox（数据模型按"视觉项"表述并区分图片与图表），无障碍文本相应改为"视觉内容预览""上一项/下一项""第 N 项，共 N 项"（可见界面不受影响）；系统开启"减少动态效果"时查看器直接显示与消失，不做动画。

- 桌面端文档目录开关改为与文件侧边栏一致的 200ms linear 切换动画：目录 DOM 在文档有目录时常驻，由 slot 与 rail 两层分别承担正文占用宽度的过渡和目录自身的滑入滑出（目录文字不再随开关重排），右下角阅读导航按钮与目录栏、正文宽度三者同步移动；收起期间目录整体 inert 并对辅助技术隐藏（内部链接不可 Tab 聚焦），窄屏（<1200px）仍共用原 Sheet 动画；侧边栏、目录与阅读导航的布局动画在系统开启"减少动态效果"时直接跳变、不再播放过渡。

- Frontmatter 新增创建/更新时间元数据摘要：`create_date` > `create_at` > `create_time` 与 `update_date` > `update_at` > `update_time` 两组别名在服务端按固定优先级归一（不受 YAML 字段顺序影响，无效高优先级别名不遮挡有效的低优先级别名），归一结果随文档 API 返回；工具栏摘要优先显示创建时间与更新时间（新增对应图标），仅当两者都不存在时才回退显示通用 `date`。别名值沿用现有日期契约——只接受有效 ISO 日期前缀并归一为 `YYYY-MM-DD`，纯时间、序列或映射值不进入摘要但仍在 Frontmatter 表格原样显示。

- 文档展示标题统一按 frontmatter `title` > 首个 H1 > 文件名的优先级解析：文件列表与文档正文顶部标题共用同一服务端规则，两者永远一致；`title` 为空或为序列/映射时回退到 H1，frontmatter YAML 无效的文件仍正常出现在文件列表（打开该文档时维持原有的 422 报错边界）。
- 正文图片悬浮时在图片底部显示 Markdown 图片名称（alt 文本）提示：与放大按钮同一时机浮现（悬停图片或键盘聚焦放大按钮），使用与界面浮层一致的主题样式，名称过长时省略号截断，宽度以视口为上限不受小图片限制；无 alt 的图片不显示提示，提示对辅助技术隐藏（图片 alt 已提供名称），重复增强不会叠加。
- Mermaid 图表接入 Lightbox 查看：图表与普通图片共用同一查看器并按文档顺序混合编号（如 图片 → 图表 → 图片 同处一个上一张/下一张序列），放大按钮与图片一致（悬停浮现、触屏常显、键盘可聚焦），缩放（最大 5 倍矢量放大不模糊）、±90° 旋转、拖动平移、Esc/关闭按钮/遮罩空白关闭与阅读位置保持全部沿用；Lightbox 中的图表以当前主题配色的 SVG 快照呈现，切换主题只重绘图表内部 SVG，图表外框与放大按钮（及按钮焦点）保持不变，未渲染成功的图表不提供放大。

### 修复

- 修复侧边栏底部 GitHub 图标显示不完整的问题：改用官方 Invertocat SVG 资源替换原有内联路径；图标尺寸、对齐与可点击区域保持不变。

- 修复侧边栏目录行悬停效果被常驻背景削弱的问题，并加深选中文档的对比度：文件、目录与多根模式的根行统一悬停样式（浅色背景 + 轻微边框阴影，目录行因吸顶所需的常驻背景此前会压制组件自带的悬停背景）；选中文档保持更深的背景、左侧 3px 标识与更明确的边框，悬停选中文档不再退回普通悬停样式。

## [0.13.0] - 2026-08-27

### 修复

- 修复全屏宽度下关闭文档目录后出现页面级横向滚动条的问题：Frontmatter 面板的基础规则声明 `width:100%`，全屏模式再叠加左右 1rem 边距使其外框比画布宽出两侧边距之和，目录栏开启时溢出暂时落入目录区域、关闭后直接产生跨越整个视口的横向滚动条；现全屏模式下正文、Frontmatter 面板与资源警告统一宽度契约（元素外框宽度 = 画布宽度 − 两侧边距），目录开关、侧边栏可见时均不再出现页面级横向溢出。
- 修复图片 Lightbox 放大后关闭按钮被图片遮挡而无法点击的问题：放大（最大 5 倍）、旋转或拖动后的图片会绘制到原始布局盒之外并形成自身的层叠上下文，而关闭按钮在 DOM 中位于舞台之前且无显式层级，会被压在图片下方；现 Lightbox 建立显式层级契约（舞台 `z-index:0`，关闭按钮与底部工具栏 `z-index:2`），任何缩放、旋转、平移状态下操作控件始终位于图片上方，缩放、旋转与拖动边界算法不变。

### 移除

- HTML 导出命令由 `m2h convert` 重命名为 `m2h export`；移除 `convert` 子命令且不保留兼容别名，其余导出参数和行为保持不变。

## [0.12.0] - 2026-08-27

### 新增

- Web 文档服务改为请求时直接读取磁盘：再次打开文档即可获取最新内容，刷新页面即可重新发现目录变更，无需重启服务。
- 根命令默认启动 Markdown 文档服务：`m2h <file|directory>...` 直接在浏览器中打开文档 WebUI，支持逗号分隔与空格分隔的多输入，保留原 `web` 子命令的全部选项（`--host`、`--port`、`--open/--no-open`、`--mode`、`--width`、`--toc`、`--glob`、`--depth`）。
- `m2h convert` 新增 `--force` 选项：输出文件已存在时覆盖写入，替代原有的交互式确认机制，脚本与 CI 调用可预测。
- 启动后不再自动打开“默认文档”：根地址 `/` 表示工作区本身，目录与多根输入显示“请选择要查看的文件”，由读者从文件树选择或经 `/doc/<路径>` 深链接直接打开；单文件输入没有文件树可选，仍自动打开唯一文件。
- 文件树初始展开策略统一：未选择文档时单根输入展开全部第一层目录（更深层保持折叠）、多根输入所有根折叠；选中或深链接打开文档时只展开其所在根与上级目录，其他根保持折叠。
- 正文图片右上角的大图查看按钮改为悬浮时显示：默认隐藏，鼠标悬停图片或键盘聚焦按钮时浮现（保留 120ms 过渡），触屏设备因无稳定悬停保持常显；按钮仍只注入一次、仅切换透明度，不影响图片列表收集与表格排序后的对应关系。
- 侧边栏与文档目录的滚动条改为滚动时显示：`ScrollArea` 新增 `scrollbarVisibility="scrolling"`，滚动中浮现、停止约 0.7 秒后淡出；文件侧边栏、桌面目录栏与窄屏目录面板共用同一行为，滚动条 DOM 保持挂载只切换透明度，不影响既有滚动几何。
- 右下角“回到顶部/前往底部”按钮在开启文档目录时左移至正文区域右下角，不再压在目录栏上；窄屏（<1200px）目录栏隐藏后恢复贴近视口右缘，侧边栏开关与宽度调整不影响该定位。

### 修复

- 删除或不存在的文档现在保留原 `/doc/...` 地址，并显示“您浏览的文档不存在或已被删除”；对应文档页、JSON API 和原始 Markdown 地址统一返回 404。

- 修复导出 HTML 中表格排序扩展的 CDN 地址无效的问题：Tablesort 的日期、数字等比较器脚本上游实际位于 `dist/sorts/`，此前拼出的 `dist/tablesort.date.js` 等地址无法加载；现导出只从 CDN 加载表格排序核心脚本（按核心默认规则比较，日期、文件大小等类型化比较器只在 WebUI 提供），导出页面引用的全部 CDN 地址已逐一验证可访问。
- 修复单文件输入删除源文件后 WebUI 不刷新的问题：单文件监听此前漏掉删除事件，`rm` 掉被服务的 Markdown 后文件树与正文保持旧状态；现删除同样触发工作区变更通知，文件树刷新为错误状态，文件重新创建后自动恢复。
- 修复 `web` 被误保留为命令字的问题：根命令的位置参数现在全部按输入路径解释，`m2h web` 即服务当前目录下的 `web` 目录（本仓库自身就有 `web/`），`m2h web docs` 被解释为两个输入，输入不存在时按常规报告找不到；此前会得到 `unknown command "web"` 错误。

### 移除

- WebUI 不再接收与展示服务器本地路径：`/api/files` 的根摘要不再返回 `absolutePath`、`pathSeparator` 与 `kind`，任何监听地址（含回环）下都不发送；侧边栏根与目录行悬停的绝对路径提示、分享菜单与文件右键菜单的“复制文档本地路径”项、目录与根行的“复制文件夹本地路径”菜单随之移除（目录行不再提供右键菜单，左键展开/折叠不变），分享菜单保留文档网页链接、Markdown 链接与 Markdown 全文。

- 移除 `m2h web` 子命令，不提供兼容别名：文档服务由根命令直接承担，`web` 不是保留字，`m2h web docs` 被解释为 `web` 与 `docs` 两个输入。
- HTML 转换从根命令移至 `m2h convert` 子命令，并收缩为单 Markdown 文件导出：目录输入、`--glob`、`--depth`、`--standalone`、`--copy-assets` 与共享 `.m2h/` 输出目录全部移除，目录输入会得到 `convert requires a Markdown file` 错误；转换成功输出简化为一行 `Wrote <HTML 绝对路径>`。
- 移除转换输出的自包含资源内嵌：KaTeX/Mermaid/表格排序运行时不再写入 HTML（共享 `.m2h/` 目录、内嵌脚本、字体 base64 与本地图片 data URI 全部移除），改为按需从 jsDelivr CDN 加载与 WebUI 内嵌运行时相同的固定版本（KaTeX 0.18.4、Mermaid 11.16.1、Tablesort 5.3.0），页面 Markdown 样式仍然内联，本地图片保留原始相对路径；查看导出文件中的公式与图表需要网络连接。
- 移除转换前的 `[y/N]` 交互式确认与 `--yes`/`-y` 选项：输出已存在且未加 `--force` 时直接报错退出。
- 转换输出的相对 Markdown 链接不再改写为 `.html`：`[b](b.md)` 保持 `href="b.md"` 原样输出，与本地图片一样保留源文件的相对引用。
- `--output`/`-o` 收紧为纯文件名：输出 HTML 始终写入 Markdown 源文件所在目录，`../x.html`、`/tmp/x.html`、`public/x.html` 等带目录分量的值会报错，避免相对图片与链接失效。

## [0.11.0] - 2026-08-23

### 新增

- Web 预览正文中的普通图片（含图片链接与原始 HTML 中的单图 `<img>` 与 `<picture>`，`<picture>` 保留源选择语义；一个链接内含多张图片时整体不提供放大按钮，Mermaid 图表除外）右上角新增放大按钮，点击进入全屏 Lightbox：底部半透明工具栏提供上一张/下一张（按图片在正文中的当前顺序编号，表格排序后仍与实际图片对应，首尾两张对应按钮禁用不循环）、缩放（1–5 倍，放大后可拖动平移查看，拖动范围自动限制在预留底部工具栏的可视区域内）、±90° 旋转（旋转后按同一可用区域重新适配，不遮挡底部工具栏）与当前位置计数，键盘 `←`/`→` 切换图片；点击图片本体保持原有行为（图片链接继续跟随链接），放大按钮独立于链接之外，不产生交互元素嵌套；关闭按钮、`Esc` 与点击遮罩空白处三种方式退出，全程不改变正文滚动位置与地址栏锚点；切换主题不关闭 Lightbox，文档保存触发的正文局部刷新会关闭并按新正文重建图片列表，切换图片时缩放、旋转与拖动状态在首帧绘制前重置。

- 目录预览侧边栏文件树支持右键菜单：文件行提供“新页面打开”（真实链接在新标签页打开渲染页）与复制文档网页链接、文档本地路径、Markdown 链接；文件夹行与多根预览的根行提供“复制文件夹本地路径”（仅目录输入的根提供，单文件根不显示）。右键不会改变当前打开的文档、目录折叠状态与阅读位置，多根预览中同名文件各自携带所在根的编号前缀互不混淆；原有的悬停 Tooltip、左键选择与折叠行为保持不变。
- Web 预览工具栏新增分享菜单（Share 图标，位于宽度调整左侧）：可复制当前文档的文档网页链接（保留当前标题位置的 hash、不携带主题/宽度/目录等个人界面参数）、文档本地路径（按根的输入类型正确拼接，单文件输入不会重复追加文件名）、Markdown 链接（`/raw/<路径>`）与 Markdown 全文（点击时才按需获取原始源文件，含 frontmatter）；复制成功后页面底部显示短暂状态提示，HTTP 环境下复制自动走 execCommand 兜底。
- Web 预览新增原始 Markdown 路由 `/raw/<路径>`：GET/HEAD 直接返回文档的原始源文件（`text/markdown; charset=utf-8`，包含 frontmatter），与 `/doc/<路径>` 渲染页一一对应，多根预览同样带根编号前缀（如 `/raw/r0/README.md`）；路径解析与 `/api/document` 共享同一安全边界，越界路径、未知根、未通过筛选的文件与非 Markdown 文件均被拒绝。`/api/files` 的根摘要同时新增 `kind` 字段（`file` 或 `directory`），标识该根的输入是单文件还是目录。

## [0.10.0] - 2026-08-22

### 新增

- Web 预览为正文为空的文档显示“当前文档无内容”提示：零字节、纯空白以及只有 frontmatter 没有正文的 Markdown 均进入该状态；仅含 frontmatter 的文档仍显示元数据折叠面板，不再渲染空白正文区域。
- 过宽代码块的横向滚动条默认视觉隐藏，鼠标悬停或键盘聚焦到代码块（如 Tab 到复制按钮）时才显示；滚动条几何尺寸保持不变，悬停切换不引起任何布局位移，Web 预览与转换输出的离线 HTML 行为一致。
- Web 预览的代码行号栏移除右侧竖向分割线，行号与代码的间距保持不变；超长代码块的折叠按钮文案调整为“展开代码（共N行）”。
- GitHub Alert 标题的类型名改为全大写（NOTE、TIP、IMPORTANT、WARNING、CAUTION），图标与文字的间距改由共享样式统一控制，Web 预览与转换输出的离线 HTML 呈现一致。
- 可排序表头的排序指示改用 Lucide arrow-down-up 矢量图标：以共享 CSS mask 实现、自动跟随主题配色，未排序列悬停或聚焦时提示、当前排序列常显，不再渲染 ↕/↑/↓ 文字字符；Web 预览与转换输出共用同一规则。
- 目录预览侧边栏的根与文件夹行悬停显示服务器本地绝对路径：`/api/files` 为每个根新增 `absolutePath`（输入的规范化绝对路径）与 `pathSeparator`（服务器平台路径分隔符），前端按服务器上报的分隔符拼接目录路径后经 Base UI Tooltip 展示，路径过长时自动换行不截断；目录行原有的原生 title 提示同时移除，避免与 Tooltip 重叠。

### 修复

- 修复超宽代码块横向滚动时复制按钮随源码一起移出可视区域的问题：复制按钮此前位于代码块滚动容器内部，横向滚动长行时会跟着内容左移，现把代码块统一包进独立的布局框架，复制按钮与折叠按钮固定在框架右上与下方，横向滚动时位置保持不变；Web 预览与转换输出的离线 HTML 行为一致，Mermaid 图表源码不再瞬时生成随即丢弃的复制按钮。
- 修复超宽代码块横向滚动时源码文字从行号栏左侧约 1rem 空隙中泄漏的问题：行号栏的吸附背景此前未覆盖代码块自身的左侧内边距区域，现把该内边距从代码块转移进行号栏，正常状态下行号与代码的水平位置保持不变，横向滚动时行号栏从代码块真正左缘开始遮挡源码。
- 修复可排序表格当前排序列的方向指示因 CSS 选择器权重被基础规则压制而始终不显示的问题（自表格排序发布以来即存在）。

## [0.9.9] - 2026-08-20

### 新增

- `m2h web` 支持一次预览多个文件或目录：`m2h web docs,wiki` 与 `m2h web docs wiki` 等价（逗号为便捷分隔符）。侧边栏以并列根分组展示（以输入名标注、重名自动加序号、根默认展开），文档与资源地址带根编号前缀（如 `/doc/r0/README.md`），两个根中的同名文档、同名图片互不混淆，Markdown 中爬出所在根的相对链接被安全拒绝；第一个输入是主根并决定默认打开的文档，搜索覆盖全部根且按根名搜索会列出该根全部文档；`--glob` 与 `--depth` 作用于全部目录输入，与单文件输入混用时不再整体报错；同一目录树的重复输入（含符号链接别名）会被拒绝。单输入预览的地址与行为保持不变。
- Web 预览右下角新增“回到顶部/前往底部”悬浮按钮组：点击平滑滚动正文到文档两端，已处于对应端点时按钮置灰提示而非隐藏；客户端晚到内容撑高文档后按钮状态自动刷新，悬浮组避开 iPhone 等设备的底部安全区。
- Web 预览的围栏代码块左侧新增行号栏：行号与代码共用同一套行高，横向滚动长行时行号固定在代码块左缘，行号不参与文本选择，复制按钮仍只复制纯源码；Mermaid 图表源码不显示行号，行号与超过 25 行折叠的行数统计共用同一算法，两者不会不一致。

### 修复

- 修复目录预览中折叠文件树目录后，侧边栏右缘偶现上一帧遗留的文件名文字碎片（需拖动侧边栏宽度或刷新才消失）的问题：滚动区域补全 Base UI 结构中缺失的内容测量层，目录折叠引起的内容高度变化会被立即重新测量与重绘；同时移除文件树子目录不必要的 1px 位移层、将侧边栏内容区改为纯裁剪边界，从 DOM/CSS 结构上消除该渲染失效诱因。

## [0.9.8] - 2026-08-19

### 修复

- 目录预览侧边栏收敛为单一滚动容器：鼠标悬停在侧边栏内滚动（包括到达顶部或底部边界后继续滚动）不再传播影响正文，正文阅读位置保持不动；文件树同时明确禁止横向滚动，深层嵌套目录不会出现左右偏移。
- 折叠已吸附置顶的目录行时，侧边栏滚动位置在渲染提交后、绘制前立即归一（钳制到折叠后的可滚动范围并复位横向偏移），不再依赖浏览器自行纠正，消除深层目录折叠后文件名残留显示的中间态；折叠包含当前文件的目录再重新展开时，当前文件会再次自动滚动到侧边栏可视区。
- 目录行吸附置顶后不再显示投影：普通目录与吸附目录视觉完全一致，吸附仍保留原有遮挡与层级行为，仅去除“悬浮卡片”观感。

## [0.9.7] - 2026-08-18

### 新增

- Web 预览中源码超过 25 行的代码块默认折叠为前 25 行并向下渐隐，底部显示“展开代码 · N 行”按钮，点击显示完整内容，展开后按钮切换为“折叠代码”可再次收起。折叠只裁剪显示高度，不改动代码内容：复制按钮仍复制完整源码，横向滚动不受影响，Mermaid 图表不参与折叠，切换明暗主题保持折叠状态不变。
- 目录预览的文件树支持目录行置顶吸附：滚动浏览深层目录内容时，各级已展开的父目录行依次吸附固定在侧边栏顶部，滚出对应子树后自然被推走；自动定位当前文件时会预留吸附目录行的高度，确保当前文件不被吸附行遮挡，正文阅读位置依旧不受影响。

## [0.9.6] - 2026-08-17

### 新增

- 目录预览的文件树在刷新直接进入深层文档或切换文档后，自动滚动到当前文件使其可见：仅调整侧边栏自身滚动位置（已可见时不移动，越界时刚好滚入），不影响正文阅读位置；侧边栏处于收起状态时不做定位，重新展开后自动补定位到当前文件。

### 修复

- Web 预览窗口宽度超过正文上限时，标准（980px）与宽（1280px）模式的正文及资源缺失提示改为在可用区域内水平居中，不再贴左显示；全屏模式保持左右 1rem 边距不变。
- 修复窄屏（<1200px）下文档目录完全不可见但工具栏按钮仍显示为开启的问题：窄屏隐藏右侧目录栏与其开关，改为工具栏按钮唤出右侧滑出的目录面板，点击目录项后面板关闭并定位到对应标题；该面板不改变 `toc` 参数，宽屏目录栏行为不变。

## [0.9.5] - 2026-08-15

### 修复

- Homebrew Formula 的验证命令会显式传入 `--yes`，兼容 m2h 转换写入前的非交互确认，保证 `brew install` 与 `brew upgrade` 可继续通过安装后测试。

## [0.9.4] - 2026-08-15

### 新增

- GFM 数据表格支持点击表头排序：第一次点击升序、再次点击降序，未排序列悬停或聚焦时显示提示箭头，当前排序列持续显示方向；表头可经 Tab 聚焦后用 Enter/Space 排序，并随排序状态维护 `aria-sort` 与提示文案。排序自动识别文本、数字、日期、文件大小与点分版本号；带自定义 class 的 HTML 表格与含链接等交互元素的表头不受影响。
- 表格排序在 Web 预览与生成的 HTML 中行为一致：预览按需从内嵌运行时加载，转换输出仅在文档包含普通表格时携带排序脚本，单文件 HTML 保持完全离线，不依赖 CDN。

- 新增扩展行内标记：`==高亮==` 渲染为 `<mark>`，`^^插入^^` 渲染为 `<ins>`，`++ctrl+alt+del++` 渲染为 Material 风格立体键帽。按键数据库与 PyMdown Extensions 的 English US 键盘一致，覆盖字母、数字、标点、导航、编辑、数字小键盘、修饰键、F1–F24、媒体、浏览器与鼠标按键；别名（如 `ctrl`、`cmd`、`pg-up`、`pipe`）统一归一化为标准键名并输出同一 CSS class（`++ctrl++` 与 `++control++` 生成的 DOM 完全一致），修饰键、导航键等在键帽上显示对应符号，标点按键的显示值经过 HTML 转义，未知按键保留原文且不带 class。
- 新增 PyMdown 风格的 Critic 协作标记：行内的 `{==高亮==}`、`{>>备注<<}`、`{--删除--}`、`{++新增++}` 与 `{~~旧~>新~~}`；块级可用 `{==`/`{++`/`{--` 单独成行开启、`==}`/`++}`/`--}` 结束来标注整段内容，段内仍按正常 Markdown 解析。
- Web 预览与转换输出的标题新增 GitHub 风格永久链接：悬停或键盘聚焦带 id 的标题时左侧浮现链接图标，点击即可定位到该标题并把地址栏 hash 同步为该标题；图标以绝对定位出现在标题左侧，出现与消失不引起正文水平位移，并对辅助技术隐藏以保持标题的可访问名称不变。`m2h` 转换出的离线 HTML 与 Web 预览行为一致。
- Web 预览与转换输出的地址栏随阅读位置自动跟随当前标题：自然滚动经过标题时 hash 实时更新（始终用 `replaceState`，不污染浏览器后退历史），置顶时清空片段；Web 预览中 `mode`/`width`/`toc` 等查询参数在 hash 变化时保持不变，关闭右侧目录后地址栏仍继续跟随。直接打开带 `#片段` 的链接在正文渲染后定位到对应章节；刷新按像素记录回到精确阅读位置，不会跳到章节开头。
- Web 预览的滚动位置恢复大幅简化：阅读正文改为随窗口滚动（工具栏与右侧目录在滚动中保持固定，视觉行为与此前一致），刷新后按当前标签页 `sessionStorage` 记录的像素偏移一次性恢复到离开位置，无记录或新窗口打开时仍按 `#片段` 定位到对应章节；不再有逐帧布局稳定检测、恢复锁定与 Scroll Anchoring 临时切换，图片等晚到内容引起的布局变化交给浏览器默认开启的 CSS Scroll Anchoring 补偿。实测表明浏览器原生 scroll restoration 不会在这种客户端渲染、内容晚到的页面上触发（对窗口与内层滚动容器均是如此），因此标签页内像素记忆予以保留。
- 默认转换动作在写入前新增终端确认：显示输入与写入目标（单文件显示推导的 `.html` 路径，目录无 `--output` 时提示就地写入可能覆盖已有文件），`[y/N]` 直接回车取消、仅 `y`/`yes` 继续；新增 `--yes`（`-y`）选项跳过确认，标准输入不是终端时（脚本、CI、管道）不加 `--yes` 直接报错退出。`m2h web` 预览不受影响。

### 修复

- Web 预览中指向当前文档的 Markdown 链接（如 `[跳转](#章节)`）与新增的标题永久链接不再触发当前文档重新拉取与正文重建：改为就地滚动到目标标题并更新 hash，跨文档链接仍正常切换并在加载后恢复 hash。
- Web 预览切换显示主题时不再销毁并重建整个 Markdown 正文：普通段落、KaTeX 公式与代码复制按钮的 DOM 保持原节点不变并保留正文内的焦点，只有 Mermaid 图表因配色固化在 SVG 中而离屏重绘后原位替换，消除主题切换时的内容跳动与焦点丢失。
- Web 预览改为加载一份稳定的 `/ui/markdown.css`：浅色与深色两套 GitHub Markdown 配色同时内嵌，分别限定在 `html:not(.dark)` 与 `html.dark` 下，由已有的 `html.dark` 类切换选择。切换主题时不再替换 `<link href>`、不再重新请求整份 Markdown CSS，从而避免整篇正文的样式重算与由此引起的阅读位置跳动；`/ui/markdown.css` 现拒绝任何查询参数，地址始终稳定。
- 工具栏的主题与文档宽度下拉菜单改为非模态（`modal={false}`），打开时不再锁定页面焦点或滚动，避免菜单对正文阅读位置的潜在扰动。
- 表格排序增强不再改变表格几何尺寸：普通 Markdown 表头的排序指示空间（`position` 与右侧 `padding`）改为在初始 HTML/CSS 阶段对全部无 class 表格静态预留，Tablesort 运行时只附加悬停、焦点等交互样式。修复可排序表格在脚本异步加载完成后列宽与表格高度突变，导致刷新后恢复的阅读位置出现可见漂移的问题。

### 移除

- 移除自建恢复状态机：不再有逐帧等待正文高度稳定的恢复循环、恢复期间的滚动与地址栏锁定，也不再在恢复期间临时关闭 CSS Scroll Anchoring；`sessionStorage` 中的 `m2h.scroll.*` 像素记录继续用于刷新恢复。

## [0.9.3] - 2026-08-13

### 新增

- 消除二进制中重复打包的 Mermaid 与 KaTeX 运行时：Web 预览界面改为通过预览服务新增的 `/runtime/*` 路由，动态加载与转换输出共用的同一份内嵌资源，`m2h` 发布包体积明显减小，预览与离线 HTML 始终运行同一版本的富内容运行时。
- Web 预览的富内容运行时改为按需加载：仅当文档包含 Mermaid 图表或数学公式分隔符时才下载对应 runtime，纯文本预览不再请求任何富内容脚本与样式，首屏开销显著降低。
- 单文件转换默认生成真正自包含的 HTML：页面样式、KaTeX/Mermaid 运行时、字体与本地图片全部内嵌为 data URI，输出旁不再生成 `.m2h/` 目录，文件可独立离线打开与分享；文档不含公式或图表时自动省去对应运行时。目录转换保持共享 `.m2h/` 运行时，并新增 `--standalone` 选项让目录中每个 HTML 也自包含。
- 内嵌的 KaTeX 字体仅保留 WOFF2 格式并同步精简样式表引用，Git 仓库、`m2h` 二进制、`.m2h/` 运行时目录与自包含 HTML 的体积进一步减小。

### 修复

- Web 预览的 Mermaid 图表不再固定为中性灰主题，改为跟随当前显示主题（浅色使用官方 `default`、深色使用官方 `dark`）；切换主题时图表自动重绘为新配色，避免在深色页面留下浅色图表。
- 转换输出的离线 HTML 中 Mermaid 图表同样跟随页面主题：`--mode light` 用 `default`、`--mode dark` 用 `dark`、`--mode auto` 在打开时按系统明暗选择，不再统一渲染为中性灰。

## [0.9.2] - 2026-08-13

### 新增

- 所有 Markdown 围栏代码块右上角新增复制按钮；Web 预览与转换后的离线 HTML 均支持，默认 HTTP 预览会自动使用兼容的复制回退。
- 目录预览侧边栏底部新增 GitHub 仓库与当前版本入口，均在新页面打开；正式版版本号直达对应 Release，开发版进入 Releases 列表。
- 统一预览架构：单文件与目录预览共用同一 React WebUI、HTTP handler 与 Markdown 渲染管线；新增 `previewScope` 表达预览的可访问文档边界，单文件不再以独立 HTML 形式渲染。Markdown 文件保存后通过 Server-Sent Events 局部刷新当前文档正文，不再整页 reload，主题、宽度、文档目录与导航状态均被保留（目录预览因此首次支持文件变更自动刷新）。`/api/files` 新增 `kind` 字段区分单文件与目录预览，单文件模式下隐藏文件侧边栏，仅保留正文与工具栏。
- `web` 子命令默认在监听成功后打开系统默认浏览器，并新增 `--no-open` 选项关闭；原 `preview --browser` 已被这一默认行为取代。

### 移除

- 移除 `view` 子命令及终端 Markdown 渲染能力。
- 移除 Glamour、Lipgloss 和 ANSI terminal renderer 相关依赖。
- CLI 重构为"默认 Markdown→HTML 转换 + `web` 浏览器预览"：移除 `convert`、`preview` 与 `version` 子命令，转换改为 `m2h <file|dir>` 默认动作，版本仅保留 `m2h --version`；root 与 `web` 选项互相隔离，`m2h web X --output`、`m2h X --port` 等跨命令选项均报错。

### 修复

- `web` 预览存在 Server-Sent Events 长连接时，按 `Ctrl+C` 会立即取消活动请求并完成优雅关闭，不再等待关闭超时后报 `context deadline exceeded`。
- 目录预览工具栏移除手动刷新入口，将文档目录开关置于最右侧并收紧图标间距，同时以 SunMoon 图标表示“跟随系统”主题。

## [0.9.1] - 2026-08-12

### 新增

- 支持通过 `brew install lz-wang/tap/m2h` 在 macOS 与 Linux 安装和升级 m2h；正式 GitHub Release 成功后会自动更新并验证 Homebrew Formula。
- 目录预览新增文档目录（TOC）面板：工具栏 TOC 按钮可切换右侧目录，初始状态由 `preview --toc`（默认 `true`）控制并随 `?toc` 写入地址栏；目录条目与正文锚点共享同一套 GitHub 兼容 id（含中文与重复标题），滚动时高亮当前小节，点击平滑跳转，窄于 1200px 时隐藏。
- 目录预览网页新增简洁的 `m>` SVG 图标，以 Markdown 折线和 HTML 尖括号表达从 Markdown 到 HTML 的转换。
- `convert`、单文件 `preview` 与目录预览默认原样渲染 Markdown 中的 HTML，并移除 `--unsafe-html` 参数；Markdown 链接和图片中的危险 URL 仍会被安全处理。
- `convert` 成功完成后会打印转换文件数、复制资源数（如有）以及生成 HTML 的绝对路径。

### 修复

- `preview` 现在会将原始 HTML 标签中的相对本地资源地址重写到安全附件路由，README 等文档使用 `<img src="...">` 时不再因页面路由前缀导致图片加载失败。
- `convert` 与目录 `preview` 的 `--depth` 最大递归深度默认值由 `2` 调整为 `4`。
- 目录预览地址栏不再为默认的自动主题与标准正文宽度写入 `mode=auto`、`width=standard`；浅色/深色主题及宽/全屏宽度仍会明确保留在 URL 中。
- 目录预览侧边栏的多级文件项保留左侧层级缩进，同时让右侧边缘与顶级文件项对齐，不再随目录深度逐层缩短选中背景。
- 目录预览拖动调整侧边栏宽度时不再反复触发 200ms 宽度过渡和整棵文件树重渲染；拖动过程直接跟随指针，仅在结束时保存最终宽度，同时保留点击开合动画。

## [0.9.0] - 2026-08-12

### 新增

- 目录预览、单文件 `preview` 与 `convert` 输出统一支持 KaTeX 数学公式（`$...$` 行内、`$$...$$` 行间）与 Mermaid 图表（` ```mermaid ` 围栏代码块）。
- `convert` 在输出目录写入共享的 `.m2h/` 富内容运行时（KaTeX、Mermaid 与字体），单文件 `preview` 经 `/m2h-assets/` 路由提供；运行时内嵌于二进制，不依赖 CDN，生成的 HTML 可离线打开。
- `convert` 与 `preview` 新增 `--width standard|wide|full`，目录预览将正文宽度同步到地址栏并停止使用旧版 localStorage 宽度。
- 目录预览侧边栏顶部支持按文档标题或文件名进行本地搜索，并自动展开匹配结果所在目录。
- 目录预览顶部显示文档标题，并让主题按钮使用当前主题对应图标。
- 目录预览支持拖动调整侧边栏宽度、标准/宽/全屏三种正文宽度，以及悬浮查看完整文件名。
- 目录预览会在浏览器本地保存侧边栏宽度和展开状态，并在文档项悬浮时同时显示文件名与标题。
- `convert`、单文件 `preview` 与目录预览统一支持 GitHub 风格标题锚点：标题自动生成 GitHub 兼容的 `id`（保留中文等 Unicode 文本，重复标题加 `-1`/`-2` 后缀），`[跳转](#小节)` 页内链接直接跳转且不被改写为 `.html`/`.md`。
- 支持 GitHub 脚注语法（`正文[^1]` 与 `[^1]: 内容`），渲染为带引用编号与回链的脚注区。
- 支持 emoji 短代码（`:smile:` 展开为 Unicode emoji，离线、无 CDN；行内代码与围栏代码块内保持字面）。
- 支持 GitHub alerts（`> [!NOTE]`、`> [!TIP]`、`> [!IMPORTANT]`、`> [!WARNING]`、`> [!CAUTION]`），渲染为带固定内嵌图标的 GitHub 风格提示框；未知标记与普通引用仍为普通引用块。
- 终端 `view` 的 emoji 短代码同样展开为 Unicode；脚注与 alerts 因终端渲染器限制安全降级为字面文本与普通引用块，正文不丢失。

### 修复

- 目录预览中 `ul`/`ol` 因被 Tailwind Preflight 重置而丢失项目符号，现在按层级恢复 `disc`/`decimal`/`circle`/`square`。
- 目录预览移除侧边栏品牌标识、标题栏内部及下方的分割线和文档标题悬浮提示，缩减文件树右侧留白，并以更明显的背景标识当前文档。
- 目录预览切换文件时不再自动将当前文件滚动到侧边栏中间，避免导航列表意外跳动。
- 目录预览请求日志简化为时间戳、客户端 IP、方法、请求位置、状态码和一位小数的毫秒耗时。
- 目录预览标题栏现在严格对齐侧边栏开关、文档标题和操作图标。
- dark 与深色系统 auto 模式下的代码块现在使用 GitHub dark 语法配色，不再因缺少颜色变量而失去高亮。
- WebDAV 构建与发布产物保留平台二进制的真实文件名及对应校验文件名，不再统一上传为 `bin` 和 `bin.sha256`。
- 目录预览切换主题、正文宽度或侧边栏等界面状态后，KaTeX 公式与 Mermaid 图表不再被重置为源码：React 不再用 `dangerouslySetInnerHTML` 与富内容渲染器争夺同一正文 DOM，改为仅持有 `<article>` 容器，正文 DOM 完全交给 KaTeX/Mermaid 渲染器管理；刷新当前文档也能正确重新渲染。
- 目录预览在文档间快速切换时，上一份文档尚未完成的 Mermaid 渲染不再把 KaTeX 错误地套用到新文档正文：富内容渲染器接受新鲜度回调，慢一拍的 Mermaid 完成后会跳过过期的 KaTeX 步骤。

## [0.8.0] - 2026-08-10

### 新增

- 提供 linux、darwin、windows 的 amd64/arm64 六种正式发布包、配套 SHA-256 校验文件，以及不依赖工作目录资源的内嵌 WebUI 二进制。
- 在每次 main 构建与正式发布中，于 Linux、macOS、Windows 原生验证 version、convert、preview 和 view 完整命令路径。

## [0.7.0] - 2026-08-10

### 新增

- 提供基于 Glamour 的单文件终端 `view`，复用标准 GFM 语法配置并渲染标题、列表、表格、代码、链接和 Unicode 内容。
- 支持 light、dark、auto 终端样式；auto 探测终端背景并在无法探测时稳定回退，非 TTY 与 `NO_COLOR` 输出不包含 ANSI 颜色。
- 在输出前完成渲染并支持取消，目录、非 Markdown 文件、读取失败和非法 mode 均返回明确的非零错误。

## [0.6.0] - 2026-08-10

### 新增

- 提供内嵌式 React 目录预览界面，包含可折叠文件树、当前文档定位、服务端标题和桌面/移动响应式布局。
- 使用浏览器历史和显式 `mode` 查询参数恢复含锚点的深链接、前进后退与 light、dark、auto 三种主题。
- 提供保留当前选择的手动刷新、删除回退，以及空目录、加载、API、文档删除和附件失败的可访问状态反馈。

## [0.5.0] - 2026-08-10

### 新增

- 提供目录 `preview` 的文件列表、最新文档正文与安全附件 API，并复用 `convert` 的 depth、glob 和符号链接发现规则。
- 提供嵌入式 SPA 深链接 fallback、稳定默认文档选择、JSON API 错误以及不记录正文的请求日志。
- 对文档和附件请求执行重复解码、绝对路径、大小写、NUL、路径穿越与符号链接逃逸防护。

## [0.4.0] - 2026-08-10

### 新增

- 提供单文件 `preview` HTTP 服务，支持 light、dark、auto 主题、优雅关闭和监听成功后可选打开浏览器。
- 监听 Markdown 父目录并通过带保活的 SSE 合并广播文档变化，兼容普通写入和 atomic rename 保存。
- 通过受 Markdown 父目录约束的 `/assets/*` 提供本地附件，并拒绝路径穿越、重复编码和越界符号链接。

## [0.3.0] - 2026-08-10

### 新增

- 提供单文件与目录 `convert`，支持稳定的 depth、doublestar glob、相对目录结构和原子 HTML 写入。
- 默认复制目录中的非 Markdown 资源，并保护输出子树、符号链接边界和目标冲突。

## [0.2.0] - 2026-08-10

### 新增

- 内置统一的标准 GFM 渲染核心，支持代码高亮、安全 raw HTML 开关、AST 链接改写和首个 H1 标题提取。
- 内置 `github-markdown-css` 5.9.0 与 light、dark、auto 三种完整页面主题。

## [0.1.0] - 2026-08-09

### 新增

- 提供可构建的 m2h CLI 骨架、完整命令帮助以及一致的 `version`、`--version` 版本输出。
