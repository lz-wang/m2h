# 更新日志

本文件记录 m2h 各版本中用户可感知的重要变化。

格式基于 [Keep a Changelog 1.1.0](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循[语义化版本 2.0.0](https://semver.org/lang/zh-CN/)。

内部重构、测试、构建和文档维护不记录，除非直接影响用户行为。

## [Unreleased]

## [0.22.0] - 2026-09-24

### 新增

- `serve` 和 `check` 新增 `--hidden` / `--no-hidden`，默认忽略隐藏文件和目录。

### 安全

- `.git`、`.ssh`、`.env` 等敏感路径始终禁止发布，且不能通过符号链接绕过。

### 修复

- 修复目录链接可能错误选择被 `.gitignore` 忽略的入口文档的问题。
- `m2h check` 现在会将 HTML、JavaScript 和 CSS 等主动 Web 文件报告为不可访问资源。

## [0.21.1] - 2026-09-23

### 新增

- `serve` 和 `check` 默认遵循 `.gitignore`，被忽略的文档和资源不再发布或检查。
- 新增 `--gitignore` / `--no-gitignore` 控制 `.gitignore` 过滤。

## [0.21.0] - 2026-09-15

### 新增

- 侧边栏文件标题和描述改为按需加载，减少大型文档目录的初始加载成本。

### 变更

- 文件快速筛选只匹配文件名和路径；标题、描述和正文搜索统一通过 `Ctrl/Cmd+K` 完成。

### 修复

- 打开文档不再等待文件列表加载，大型目录中的文档可以更快显示。
- 文件列表加载失败时不再清空已经打开的文档。
- 优化本地目录链接解析，避免为单个链接扫描整个工作区。

### 移除

- `/api/files` 响应不再包含 `title` 与 `description` 字段，展示元数据改由 `/api/file-metadata` 按需获取。

## [0.20.1] - 2026-09-11

### 修复

- 修复 GFM Alert 标题的类型图标跑到标题最右侧的问题。

## [0.20.0] - 2026-09-10

### 新增

- 新增 `--cdn` / `--no-cdn`，默认关闭；启用后 WebUI 从 jsDelivr CDN 加载图表、公式与表格排序依赖。

### 变更

- 文档目录开关统一为 `--toc` / `--no-toc`，默认开启；`--toc=true` / `--toc=false` 写法不再接受，关闭目录请使用 `--no-toc`。
- 正文图片、SVG、Mermaid 与 Vega-Lite 图表统一居中排列。
- Lightbox 查看 SVG 图片时不再显示底部图片信息。

### 修复

- 修复 Lightbox 中图片被内边距与底部信息区裁切的问题，缩放与拖动区域覆盖整个视口。

## [0.19.1] - 2026-09-10

### 新增

- `m2h check` 新增三条默认开启的 warning：Frontmatter 标题与正文 H1 不一致（`frontmatter.title-mismatch`）、标签数量不在 1–5 个（`frontmatter.tags-count`）、文档末尾多余空行（`document.trailing-blank-lines`）。

### 修复

- 修复 WebUI 中本地目录链接被误送到 `/assets/` 的问题，现在正确打开目录入口文档。
- 修复 `$...$` 与 `$$...$$` 数学公式被误判为引用、标题等 Markdown 语法的问题。

## [0.19.0] - 2026-09-09

### 新增

- WebUI 图片支持延迟加载，减少长文档首次打开时的资源请求。
- Lightbox 新增图片名称、尺寸和格式信息。

### 变更

- 图片悬浮信息最大宽度调整为文档宽度的 80%。
- Mermaid 和 Vega-Lite 的 Lightbox 使用全屏图表背景。

### 修复

- 修复图片加载失败时占位图无法显示的问题。
- 统一普通图片、Mermaid 和 Vega-Lite 与正文之间的上下间距。
- 减小文档底部 `Powered by m2h` 下方留白。

## [0.18.1] - 2026-09-07

### 修复

- 修复阅读顶栏背景半透明导致正文从顶栏下方透出的问题。
- 修复深色模式下移动端目录背景与桌面端不一致的问题。
- 修复侧边栏多行文件提示的箭头可能指向错误文件的问题，并统一标题与描述的文字颜色。
- 统一代码块与前后正文的上下间距。
- 统一侧边栏、目录栏与正文的背景色。

## [0.18.0] - 2026-09-07

### 新增

- 文档末尾新增 `Powered by m2h` 页脚，显示版本并链接到对应 GitHub Release。

### 变更

- 阅读顶栏背景改为半透明。
- 图片悬浮提示合并为单行「名称 (尺寸, 格式)」。

### 修复

- 修复正文滚动到文档底部时右侧目录栏整体向上漂移的问题。

## [0.17.2] - 2026-09-05

### 修复

- 修复移动端首次打开文件导航后无法直接上下滑动的问题。
- 修复移动端文件导航打开后当前文件不会自动定位到可视区域的问题。
- 修复移动端目录过长时无法触摸滚动、超长目录标题产生横向滚动的问题。

## [0.17.1] - 2026-09-04

### 变更

- 文件筛选改用漏斗图标，提示文案标明 `Ctrl/Cmd+K` 全文搜索入口。
- 主题模式控件移至顶栏，侧边栏过窄时自动隐藏版本号。

### 修复

- 修复 Lightbox 中 Mermaid 图表丢失配色与字体的问题。
- 修复 Lightbox 中图表无法缩放、平移与选择文字的问题。
- 修复 Lightbox 中图表放大后模糊的问题。

### 移除

- 移除 WebUI 顶栏的全文搜索与分享按钮；全文搜索仍可通过 `Ctrl+K` / `Cmd+K` 打开，文件树右键菜单的链接复制保持可用。

## [0.17.0] - 2026-09-04

### 新增

- WebUI 新增工作区全文搜索：`Ctrl+K` / `Cmd+K` 打开，搜索标题、路径、标签、描述与正文，可直接跳转到匹配章节。
- 新增 `GET /api/search?q=<query>` 搜索 API。

## [0.16.1] - 2026-09-04

### 新增

- Frontmatter 新增 `description` 字段，在侧边栏文件提示中展示并参与筛选。
- 图片悬浮提示新增尺寸与格式信息。
- 渲染失败的 Mermaid / Vega-Lite 图表与加载失败的图片统一显示占位图，不再显示源码文本或破图图标。
- WebUI 访问根地址 `/` 时自动打开默认文档，并规范化为可分享的深链接。

### 变更

- 移动端窄屏下正文宽度档位统一为全宽。

### 修复

- 修复 Lightbox 中图表画布不随阅读主题变化的问题。
- 修复链接内包含多张图片时缺少悬浮提示与失败占位的问题。

## [0.16.0] - 2026-09-03

### 新增

- 新增 Vega-Lite 统计图表支持：` ```vega-lite ` 代码块中的 JSON spec 渲染为交互式 SVG 图表，数据需内嵌在 `data.values`。

### 修复

- 修复图表运行时加载期间快速切换主题时 Vega-Lite 图表可能停留在源码块或渲染损坏的问题。

## [0.15.2] - 2026-08-30

### 新增

- Web 服务新增统一的浏览器安全响应头（CSP、`X-Content-Type-Options`、`Referrer-Policy` 等）。
- 新增 `GET /healthz` 健康检查端点，适合 systemd 与 Uptime Kuma 等监控探测。
- HTTP 服务新增超时与请求头大小限制，支持长期常驻运行。

### 安全

- 目录中的隐藏路径（`.env`、`.git/` 等）不再出现在文件树与文档、附件路由中。
- 修复可见符号链接绕过发布边界读取隐藏文件的问题。
- `/assets` 不再提供 HTML、JavaScript、CSS 等主动 Web 内容，避免同源脚本执行。

## [0.15.1] - 2026-08-30

### 新增

- Markdown 引用支持以单个 `/` 开头的当前文档 root 相对路径（如 `/docs/guide.md`）。

### 修复

- 修复多根工作区中越界的本地链接可能被浏览器解析到其他根的问题。

## [0.15.0] - 2026-08-30

### 新增

- 新增 `m2h check` 子命令：检查 Markdown 的 Frontmatter、结构、本地引用与锚点，支持 `--depth`、`--glob`、`--format`、`--strict`。
- `m2h check` 支持 `--enable` / `--disable` 选择规则，规则总数扩展到 25 条。
- `m2h check` 的文本报告在交互式终端按等级着色。
- Mermaid 新增 ZenUML 时序图支持。
- Lightbox 新增鼠标滚轮平滑缩放。
- WebUI 中跨站外链自动在新标签页打开。

### 修复

- 修复同一段落中多个美元金额被误渲染为行内公式的问题。
- 修复 ZenUML 图表污染阅读主题的问题。
- 修复 `m2h export` 不带参数时无法显示帮助的问题。

## [0.14.0] - 2026-08-28

### 新增

- Mermaid 图表接入 Lightbox，与图片共用查看器，支持缩放、旋转与拖动。
- 图片悬浮时显示图片名称（alt 文本）提示。
- Frontmatter 新增创建/更新时间摘要，支持 `create_date` / `update_at` 等别名。
- 文档展示标题统一按 Frontmatter `title` > 首个 H1 > 文件名解析。
- Lightbox 新增进出场动画，文档目录开关与侧边栏一致使用切换动画。

### 修复

- 修复侧边栏 GitHub 图标显示不完整的问题。
- 加深文件树悬停与选中状态的对比度。

## [0.13.0] - 2026-08-27

### 修复

- 修复全屏宽度下关闭文档目录后出现横向滚动条的问题。
- 修复 Lightbox 放大后关闭按钮被图片遮挡而无法点击的问题。

### 移除

- HTML 导出命令由 `m2h convert` 重命名为 `m2h export`，不保留兼容别名；升级脚本需要同步修改命令名称。

## [0.12.0] - 2026-08-27

### 新增

- `m2h <file|directory>` 直接启动文档服务，支持多输入与原有 `--host`、`--port` 等选项。
- 文档服务改为请求时直接读取磁盘，修改即时生效，无需重启。
- `m2h export` 新增 `--force`，覆盖已存在的输出文件。
- 正文图片的放大按钮改为悬浮时显示。

### 变更

- 根地址 `/` 不再自动打开默认文档，改为由文件树选择或深链接打开。
- 导出 HTML 的公式与图表运行时改为从 jsDelivr CDN 按需加载，不再内嵌，查看需要网络连接。
- 导出 HTML 的 Markdown 链接保持原始相对地址，不再改写为 `.html`。
- `--output` 只接受纯文件名，输出始终写入源文件所在目录。

### 修复

- 修复导出 HTML 中表格排序扩展 CDN 地址无效的问题。
- 修复单文件输入删除源文件后 WebUI 不刷新的问题。
- 删除或不存在的文档统一保留原地址并返回 404。

### 移除

- 移除 `m2h web` 子命令与转换前的交互确认（`--yes`）。
- HTML 导出收缩为单 Markdown 文件，目录输入与 `--glob`、`--depth`、`--standalone`、`--copy-assets` 移除。
- WebUI 不再展示服务器本地路径，本地路径复制相关菜单随之移除。

## [0.11.0] - 2026-08-22

### 新增

- 正文图片新增放大按钮与全屏 Lightbox：上一张/下一张、1–5 倍缩放、±90° 旋转与拖动平移。
- 新增原始 Markdown 路由 `/raw/<路径>`，与文档页一一对应。
- 侧边栏文件树支持右键菜单：新页面打开与复制链接。
- 工具栏新增分享菜单：复制文档网页链接、Markdown 链接与 Markdown 全文。

## [0.10.0] - 2026-08-21

### 新增

- 为无正文的文档显示「当前文档无内容」提示。

### 变更

- GitHub Alert 标题的类型名改为全大写（NOTE、TIP 等）。
- 可排序表头的方向指示改用矢量图标，自动跟随主题配色。
- 过宽代码块的横向滚动条默认隐藏，悬停或聚焦时显示。

### 修复

- 修复超宽代码块横向滚动时复制按钮随源码移出可视区域、源码从行号栏边缘泄漏的问题。
- 修复可排序表格当前排序列方向指示始终不显示的问题。

## [0.9.9] - 2026-08-20

### 新增

- `m2h web` 支持一次预览多个文件或目录，侧边栏按根分组，同名文档互不混淆。
- 围栏代码块左侧新增行号栏，横向滚动时行号固定。
- 右下角新增「回到顶部 / 前往底部」悬浮按钮。

### 修复

- 修复折叠文件树目录后侧边栏偶现文字残影的问题。

## [0.9.8] - 2026-08-19

### 修复

- 修复在侧边栏内滚动会影响正文阅读位置的问题。
- 修复深层目录折叠后偶现文件名残留显示的问题。
- 吸附置顶的目录行不再显示投影。

## [0.9.7] - 2026-08-18

### 新增

- 超过 25 行的代码块默认折叠并渐隐，点击可展开完整内容。
- 文件树支持目录行置顶吸附，滚动时父目录依次固定在侧边栏顶部。

## [0.9.6] - 2026-08-17

### 新增

- 切换或深链接打开文档后，文件树自动滚动到当前文件使其可见。

### 修复

- 修复窗口过宽时正文贴左显示的问题，改为在可用区域水平居中。
- 修复窄屏下文档目录不可见但工具栏开关仍显示为开启的问题。

## [0.9.5] - 2026-08-15

### 修复

- 修复 Homebrew 安装后验证与写入确认不兼容，导致 `brew install` / `brew upgrade` 失败的问题。

## [0.9.4] - 2026-08-15

### 新增

- GFM 数据表格支持点击表头排序，自动识别文本、数字、日期与版本号。
- 新增扩展行内标记：`==高亮==`、`^^插入^^` 与 `++ctrl+c++` 风格键帽。
- 新增 PyMdown 风格 Critic 协作标记（`{++新增++}`、`{--删除--}` 等）。
- 标题新增 GitHub 风格永久链接，悬停浮现链接图标。
- 转换写入前新增终端确认，`--yes` 跳过。

### 变更

- 地址栏随阅读位置自动跟随当前标题，刷新后按像素恢复精确阅读位置。

### 修复

- 修复切换主题时正文销毁重建导致的跳动与焦点丢失。
- 修复可排序表格脚本加载完成后列宽突变、恢复的阅读位置漂移的问题。

## [0.9.3] - 2026-08-13

### 新增

- 单文件转换默认生成自包含 HTML，样式、运行时与图片全部内嵌，可独立离线打开。

### 变更

- Web 预览的富内容运行时改为按需加载，纯文本文档不再请求图表与公式脚本。
- Mermaid 与 KaTeX 运行时不再重复内嵌于二进制，发布包体积明显减小。

### 修复

- 修复 Mermaid 图表不跟随阅读主题的问题，浅色与深色分别使用官方 default / dark 配色。

## [0.9.2] - 2026-08-13

### 新增

- 所有围栏代码块新增复制按钮。
- Markdown 文件保存后自动局部刷新正文，不再整页重载。
- `web` 子命令默认在监听成功后打开浏览器，新增 `--no-open` 关闭。
- 侧边栏底部新增 GitHub 仓库与版本入口。

### 修复

- 修复 `web` 预览存在长连接时 `Ctrl+C` 无法立即优雅关闭的问题。

### 移除

- 移除 `view` 子命令与终端 Markdown 渲染能力，及 Glamour 等相关依赖。
- CLI 重构为「默认 Markdown→HTML 转换 + `web` 预览」：移除 `convert`、`preview` 与 `version` 子命令。

## [0.9.1] - 2026-08-12

### 新增

- 支持通过 `brew install lz-wang/tap/m2h` 在 macOS 与 Linux 安装和升级。
- 目录预览新增文档目录（TOC）面板，滚动时高亮当前小节。

### 变更

- 默认原样渲染 Markdown 中的 HTML，移除 `--unsafe-html` 参数。
- `--depth` 默认值由 `2` 调整为 `4`。

### 修复

- 修复 raw HTML 中的相对资源地址导致图片加载失败的问题。

## [0.9.0] - 2026-08-12

### 新增

- 支持 KaTeX 数学公式（`$...$`、`$$...$$`）与 Mermaid 图表（` ```mermaid `）。
- 支持 GitHub 脚注、emoji 短代码与 GitHub alerts（`> [!NOTE]` 等）。
- 支持 GitHub 兼容标题锚点，中文标题可直接页内跳转。
- 新增 `--width standard|wide|full` 正文宽度选项与侧边栏宽度拖动调整。
- 目录预览侧边栏支持按标题或文件名搜索。

### 修复

- 修复列表丢失项目符号的问题。
- 修复切换主题或界面状态后公式与图表被重置为源码的问题。
- 修复深色模式下代码块失去语法高亮的问题。

## [0.8.0] - 2026-08-10

### 新增

- 提供 linux / darwin / windows 的 amd64 / arm64 六种发布包与 SHA-256 校验文件。

## [0.7.0] - 2026-08-10

### 新增

- 新增基于 Glamour 的单文件终端 `view` 命令，支持 light / dark / auto 终端样式。

## [0.6.0] - 2026-08-10

### 新增

- 新增内嵌式 React 目录预览界面：可折叠文件树、深链接恢复与 light / dark / auto 主题。

## [0.5.0] - 2026-08-10

### 新增

- 目录 `preview` 提供文件列表、文档正文与安全附件 API。
- 对文档与附件请求执行路径穿越、重复解码与符号链接逃逸防护。

## [0.4.0] - 2026-08-10

### 新增

- 新增单文件 `preview` HTTP 服务，支持 light / dark / auto 主题与监听后打开浏览器。
- 监听 Markdown 变化并通过 SSE 广播刷新，本地附件经 `/assets/*` 受约束提供。

## [0.3.0] - 2026-08-10

### 新增

- 新增单文件与目录 `convert`：支持 depth、doublestar glob 与原子写入，默认复制非 Markdown 资源。

## [0.2.0] - 2026-08-10

### 新增

- 内置标准 GFM 渲染核心，支持代码高亮与安全 raw HTML 开关。
- 内置 `github-markdown-css` 与 light / dark / auto 三种页面主题。

## [0.1.0] - 2026-08-09

### 新增

- 提供可构建的 m2h CLI 骨架、完整命令帮助与 `--version` 版本输出。
