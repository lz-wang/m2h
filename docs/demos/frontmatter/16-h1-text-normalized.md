---
tags: [标题, 归一化]
---

#  **快速**   开始 &amp;  部署

H1 文本包含行内标记、HTML 实体与多余空白。

预期表现：

- 显示标题为 **快速 开始 & 部署**：行内标记（加粗等）剥离为纯文本，`&amp;` 解码为 `&`，连续空白折叠为单个空格。
- 目录（TOC）中的同名条目应用完全相同的归一规则，两处永不漂移。
- 标签照常显示在摘要行。
