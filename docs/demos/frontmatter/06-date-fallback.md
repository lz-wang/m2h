---
title: 通用 date 兜底
date: 2026-06-01
tags: 单个标签
---

# 通用 date 兜底

没有 `create_*` / `update_*` 别名，只有通用 `date` 与一个标量形式的 tag。

预期表现：

- 摘要以日历图标兜底显示 **2026-06-01**。
- 标量 tag `单个标签` 直接作为唯一标签显示，不需要写成序列。
- 表格列出 `date` 与 `tags` 两个条目。
